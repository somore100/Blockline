# Blockline dependencies
#
# Note: tkinter is NOT pip-installable. On Linux it's a separate OS
# package (see setup_venv.sh for the apt/dnf/pacman commands).
# On Windows/macOS python.org installers it's already bundled.

Pillow>=10.0.0

# --- build-time only (not needed to just run the app) ---
pyinstaller>=6.0.0
